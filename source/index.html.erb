---
title: Home
description: |-
  Homepage of Abhishek Dey Das.
---
<p>Hi, I am Abhishek (<em>pronounced uh - b h ee - sh eh k</em>) and welcome to my blog. This site is the captain's log for my experiments with technology, photography and my opinions on topics that either interests or fascinates me. Enjoy!</p>

<h2>Recent photos</h2>
<div align="center">
<% page_articles.each_with_index do |article, i| %>
    <% if article.data.category == 'Photography' %>
      <a href="<%= article.url %>"><img src="<%= article.data.coverimage %>" style="height: 200px;" height="200px" title="<%= article.title %>" alt="<%= article.title %>" class="img-thumbnail" /></a>
    <% end %>  
<% end %>
</div>

<h2>Blog posts</h2>
<table class="table table-striped table-hover ">
  <thead>
    <tr>
      <th>Date</th>
      <th>Title</th>
      <th>Category</th>
    </tr>
  </thead>
  <tbody>
    
  <% page_articles.each_with_index do |article, i| %>
    <tr><td><%= article.date.strftime('%b %e, %Y') %></td> <td><%= link_to article.title, article %></td>
      <td><%= article.data.category %></td></tr>
  <% end %>
    
  </tbody>
</table>